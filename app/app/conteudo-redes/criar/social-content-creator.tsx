"use client";
import Link from "next/link";import{useMemo,useState}from"react";
const typeLabels:Record<string,string>={anuncio:"Anúncio",comunicado:"Comunicado",sazonal:"Sazonal",homenagem:"Institucional / Homenagem"};type Props={initialType:string};type GenerationResponse={image?:string;error?:string};
export function SocialContentCreator({initialType}:Props){const[type,setType]=useState(initialType),[occasion,setOccasion]=useState(type==="homenagem"?"Dia das Mães":""),[objective,setObjective]=useState(""),[message,setMessage]=useState(""),[format,setFormat]=useState<"feed"|"story">("feed"),[style,setStyle]=useState("acolhedor e elegante"),[quality,setQuality]=useState<"low"|"medium"|"high">("medium"),[image,setImage]=useState<string|null>(null),[error,setError]=useState<string|null>(null),[loading,setLoading]=useState(false),[headline,setHeadline]=useState(type==="comunicado"?"COMUNICADO":""),[line1,setLine1]=useState(type==="comunicado"?"No feriado de":""),[line2,setLine2]=useState(""),[line3,setLine3]=useState(""),[footer,setFooter]=useState(type==="comunicado"?"Agradecemos a compreensão":"");const title=useMemo(()=>typeLabels[type]??"Conteúdo para redes",[type]),isComunicado=type==="comunicado";async function generate(){setError(null);setLoading(true);try{const response=await fetch("/api/openai/generate-social-image",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type,occasion,objective,message,format,style,quality,backgroundOnly:true})});const data=await response.json() as GenerationResponse;if(!response.ok||!data.image)throw new Error(data.error||"Não foi possível gerar a imagem.");setImage(data.image)}catch(err){setError(err instanceof Error?err.message:"Erro inesperado ao gerar a imagem.")}finally{setLoading(false)}}const caption=isComunicado?[headline,line1,line2,line3,footer].filter(Boolean).join("\n"):message||occasion;const publicationHref=`/app/publicacoes/nova?type=${format}&caption=${encodeURIComponent(caption)}`;return <div><header className="page-head"><div><Link className="muted back-link" href="/app/conteudo-redes">← Conteúdo para Redes</Link><h1>Criar {title}</h1><p className="muted" style={{margin:0}}>A IA pode gerar o visual; textos e identidade ficam sob controle do aplicativo.</p></div><Link className="btn" href="/app/conteudo-redes/marca">Kit da marca</Link></header><div className="creator-layout"><section className="card"><div className="section-title-row"><div><small className="eyebrow">EDITOR</small><h2>Proposta da arte</h2></div></div><div className="form"><label className="field"><span>Tipo</span><select className="input" value={type} onChange={e=>setType(e.target.value)}><option value="anuncio">Anúncio</option><option value="comunicado">Comunicado</option><option value="sazonal">Sazonal</option><option value="homenagem">Institucional / Homenagem</option></select></label>{isComunicado?<><label className="field"><span>Título</span><input className="input" value={headline} onChange={e=>setHeadline(e.target.value)}/></label><label className="field"><span>Linha de contexto</span><input className="input" value={line1} onChange={e=>setLine1(e.target.value)} placeholder="No feriado de"/></label><label className="field"><span>Data / destaque</span><input className="input" value={line2} onChange={e=>setLine2(e.target.value)} placeholder="21 DE ABRIL · TERÇA-FEIRA"/></label><label className="field"><span>Mensagem principal</span><input className="input" value={line3} onChange={e=>setLine3(e.target.value)} placeholder="não haverá expediente"/></label><label className="field"><span>Rodapé</span><input className="input" value={footer} onChange={e=>setFooter(e.target.value)}/></label></>:<><label className="field"><span>Ocasião / tema</span><input className="input" value={occasion} onChange={e=>setOccasion(e.target.value)} placeholder="Ex.: Dia das Mães"/></label><label className="field"><span>Objetivo</span><textarea className="input" rows={3} value={objective} onChange={e=>setObjective(e.target.value)}/></label><label className="field"><span>Mensagem</span><textarea className="input" rows={3} value={message} onChange={e=>setMessage(e.target.value)}/></label></>}<div className="form-grid compact"><label className="field"><span>Formato</span><select className="input" value={format} onChange={e=>setFormat(e.target.value as "feed"|"story")}><option value="feed">Feed · 1080×1080</option><option value="story">Story · 1080×1920</option></select></label><label className="field"><span>Qualidade IA</span><select className="input" value={quality} onChange={e=>setQuality(e.target.value as "low"|"medium"|"high")}><option value="low">Baixa · rascunho</option><option value="medium">Média</option><option value="high">Alta · final</option></select></label></div>{!isComunicado&&<><label className="field"><span>Direção visual</span><input className="input" value={style} onChange={e=>setStyle(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn primary" onClick={generate} disabled={loading||!occasion.trim()} type="button">{loading?"Gerando fundo...":image?"Gerar outro fundo com IA":"Gerar fundo com IA"}</button></>}</div></section><section className="card creator-preview"><div className="section-title-row"><div><small className="eyebrow">PRÉVIA</small><h2>{format==="feed"?"Feed":"Story"}</h2></div><span className="pill">{isComunicado?"Template oficial":"Composição"}</span></div><div className={`social-preview ${format} ${isComunicado?"comunicado-preview":""}`} style={!isComunicado&&image?{backgroundImage:`url(${image})`}:undefined}>{isComunicado?<><div className="com-pattern">VAREJÃO<br/>POPULAR<br/>VAREJÃO<br/>POPULAR</div><div className="com-copy"><strong>{headline||"COMUNICADO"}</strong><em>{line1}</em><b>{line2}</b><i>{line3}</i></div><div className="com-footer">{footer}<span className="vp-social-logo">VP</span></div></>:<div className="social-overlay"><strong>{occasion||"Sua ocasião"}</strong><span>{message||"Sua mensagem aparecerá aqui"}</span><div className="vp-social-logo">VP</div></div>}</div><div className="preview-actions"><button className="btn" type="button" onClick={()=>window.print()}>Imprimir / salvar</button><Link className="btn primary" href={publicationHref}>Enviar para Publicações</Link></div><p className="muted preview-note">O envio abre um rascunho em Publicações com formato e legenda pré-preenchidos; a mídia final pode ser adicionada após a revisão.</p></section></div></div>}
