"use client";
import {useMemo,useState} from "react";
type Product={id:string;ean:string|null;name:string;brand:string|null;specification:string|null;category:string|null;unit:string|null;sale_price:number|null};type Entry={productId:string;name:string;brand:string;specification:string;normalPrice:string;offerPrice:string;expiry:string;ean:string;unit:string};const emptyEntry=():Entry=>({productId:"",name:"",brand:"",specification:"",normalPrice:"",offerPrice:"",expiry:"",ean:"",unit:"UN"});
function splitPrice(value:string){const clean=value.replace(/[^0-9,]/g,"");const [a="0",b="00"]=clean.split(",");return{major:a||"0",minor:(b+"00").slice(0,2)}}
function eanBars(code:string){const d=code.replace(/\D/g,"");if(d.length!==13)return null;const L=["0001101","0011001","0010011","0111101","0100011","0110001","0101111","0111011","0110111","0001011"],G=["0100111","0110011","0011011","0100001","0011101","0111001","0000101","0010001","0001001","0010111"],R=["1110010","1100110","1101100","1000010","1011100","1001110","1010000","1000100","1001000","1110100"],P=["LLLLLL","LLGLGG","LLGGLG","LLGGGL","LGLLGG","LGGLLG","LGGGLL","LGLGLG","LGLGGL","LGGLGL"][Number(d[0])];let bits="101";for(let i=1;i<=6;i++)bits+=(P[i-1]==="L"?L:G)[Number(d[i])];bits+="01010";for(let i=7;i<=12;i++)bits+=R[Number(d[i])];return bits+"101"}
function Barcode({code}:{code:string}){const bits=eanBars(code);if(!bits)return <div className="barcode-fallback">{code||"EAN/GTIN"}</div>;return <div><svg className="barcode-svg" viewBox="0 0 95 38" preserveAspectRatio="none" aria-label={code}>{bits.split("").map((b,i)=>b==="1"?<rect key={i} x={i} y="0" width="1" height="30" fill="#000"/>:null)}</svg><div className="barcode-number">{code}</div></div>}
export function ValidityFlyerGenerator({products}:{products:Product[]}){const[entries,setEntries]=useState<Entry[]>(()=>[emptyEntry(),emptyEntry(),emptyEntry(),emptyEntry()]);const options=useMemo(()=>products.map(p=>({value:p.id,label:[p.name,p.brand,p.specification].filter(Boolean).join(" · ")})),[products]);function patch(index:number,values:Partial<Entry>){setEntries(old=>old.map((e,i)=>i===index?{...e,...values}:e))}function selectProduct(index:number,id:string){const p=products.find(x=>x.id===id);if(!p){setEntries(old=>old.map((e,i)=>i===index?emptyEntry():e));return}patch(index,{productId:id,name:p.name,brand:p.brand??"",specification:p.specification??p.unit??"",normalPrice:p.sale_price!=null?p.sale_price.toFixed(2).replace(".",","):"",ean:p.ean??"",unit:p.unit??"UN"})}return <><section className="card no-print" style={{marginBottom:18}}><div className="section-title-row"><div><small className="eyebrow">DADOS DA FOLHA</small><h2>4 ofertas de validade</h2></div><button className="btn primary" type="button" onClick={()=>window.print()}>Imprimir / salvar PDF</button></div><p className="muted">Selecione produtos do catálogo ou ajuste qualquer informação manualmente. Validade e preço de oferta pertencem somente a esta folha.</p><div className="validity-editor-grid">{entries.map((e,index)=><div className="validity-editor" key={index}><strong>Oferta {index+1}</strong><label className="field"><span>Produto do catálogo</span><select className="input" value={e.productId} onChange={ev=>selectProduct(index,ev.target.value)}><option value="">Preencher manualmente</option>{options.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></label><div className="form-grid compact"><label className="field"><span>Produto / tipo</span><input className="input" value={e.name} onChange={ev=>patch(index,{name:ev.target.value})}/></label><label className="field"><span>Marca</span><input className="input" value={e.brand} onChange={ev=>patch(index,{brand:ev.target.value})}/></label><label className="field"><span>Especificação</span><input className="input" value={e.specification} onChange={ev=>patch(index,{specification:ev.target.value})}/></label><label className="field"><span>Validade</span><input className="input" placeholder="20/05/26" value={e.expiry} onChange={ev=>patch(index,{expiry:ev.target.value})}/></label><label className="field"><span>Preço normal</span><input className="input" inputMode="decimal" value={e.normalPrice} onChange={ev=>patch(index,{normalPrice:ev.target.value})}/></label><label className="field"><span>Preço oferta</span><input className="input" inputMode="decimal" value={e.offerPrice} onChange={ev=>patch(index,{offerPrice:ev.target.value})}/></label><label className="field"><span>Código de barras</span><input className="input" inputMode="numeric" value={e.ean} onChange={ev=>patch(index,{ean:ev.target.value})}/></label><label className="field"><span>Unidade</span><input className="input" value={e.unit} onChange={ev=>patch(index,{unit:ev.target.value})}/></label></div></div>)}</div></section><div className="validity-sheet-wrap"><div className="validity-sheet">{entries.map((e,index)=>{const price=splitPrice(e.offerPrice);return <article className="validity-ticket" key={index}><div className="validity-offer">OFERTA</div><div className="validity-ribbon">VALIDADE <span>!</span></div><div className="validity-yellow"><div className="ticket-name">{e.name||"PRODUTO"}</div><div className="ticket-brand">{e.brand||"MARCA"}</div><div className="ticket-spec">{e.specification||"ESPECIFICAÇÃO"}</div><div className="ticket-expiry">VALIDADE {e.expiry||"__/__/__"}</div><div className="ticket-price"><small>R$</small><strong>{price.major}</strong><div><b>,{price.minor}</b><span>{e.unit||"UN"}</span></div></div><div className="ticket-bottom"><div className="normal-price"><small>PREÇO NORMAL:</small><b>R$ {e.normalPrice||"0,00"}</b></div><div className="barcode-box"><small>CÓDIGO DE BARRAS</small><Barcode code={e.ean}/></div></div></div><footer><span className="tag-icon">%</span><b>Oferta válida enquanto<br/>durarem os estoques</b><span className="vp-mark">VP</span></footer></article>})}</div></div></>}
